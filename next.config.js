/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['unicdata.pythonanywhere.com','www.google.com','encrypted-tbn0.gstatic.com' ],
      },
}

module.exports = nextConfig
